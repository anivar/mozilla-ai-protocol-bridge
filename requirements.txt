# Core dependencies
pydantic>=2.0.0
uvicorn>=0.20.0
starlette>=0.25.0

# Optional: ACP SDK for full ACP features
# agntcy-acp>=0.1.0

# Optional: Real MCP integration
# mcp>=0.1.0

# Development dependencies (optional)
pytest>=7.0.0
httpx>=0.24.0  # for testing + production HTTP client features (from rejected PR mozilla-ai/any-llm#254)